# `IsInspectorVisible`

**Since:** `IVPrismaUI1`

```cpp
virtual bool IsInspectorVisible(PrismaView view) noexcept = 0;
```

Returns `true` if the inspector is currently visible for this view.

## See also

`SetInspectorVisibility`.
