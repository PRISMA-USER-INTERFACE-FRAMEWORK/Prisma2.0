# `HasFocus`

**Since:** `IVPrismaUI1`

```cpp
virtual bool HasFocus(PrismaView view) noexcept = 0;
```

Returns `true` if this view currently has input focus.

## See also

`Focus`, `Unfocus`, `HasAnyActiveFocus`.
