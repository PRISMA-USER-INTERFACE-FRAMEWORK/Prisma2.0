# `SetOrder`

**Since:** `IVPrismaUI1`

```cpp
virtual void SetOrder(PrismaView view, int order) noexcept = 0;
```

Sets the view's rendering z-order among other visible views. Higher values render on top. Default
is `0`.

## See also

`GetOrder`.
