# `GetViewRole`

**Since:** `IVPrismaUI10`

```cpp
virtual ViewRole GetViewRole(PrismaView view) noexcept = 0;
```

See [`SetViewRole`](SetViewRole.md) — the role enum, usage, and both getter and setter are documented together.
